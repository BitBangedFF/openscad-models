// common_functions.scad

function get_visual_overrun() = 0.1;

function ft_to_cm(ft) = ft * 30.48;

function cm_to_ft(cm) = cm / 30.48;

function in_to_cm(in) = in * 2.54;

function cm_to_in(cm) = cm / 2.54;
